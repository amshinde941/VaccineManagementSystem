import React from "react";
import Layout from "../Layout/Layout";

const Home = ({ data }) => {
  return (
    <Layout>
        <div>
            home
        </div>
    </Layout>
  );
};

export default Home;
